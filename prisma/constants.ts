import { brandsData } from '../shared/data/brands.data'
import { newsData } from '../shared/data/news.data'
import { carsData } from '../shared/data/cars.data'
import { newCarsData } from '../shared/data/newCars.data'

export const newCars = [...newCarsData]

export const cars = [...carsData]

export const news = [...newsData]

export const brands = [...brandsData]
